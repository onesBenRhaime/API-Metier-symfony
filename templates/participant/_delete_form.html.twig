<form method="post" action="{{ path('app_participant_delete', {'idparticipant': participant.idparticipant}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participant.idparticipant) }}">
    <button class="btn">Delete</button>
</form>
