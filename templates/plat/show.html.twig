{% extends 'base.html.twig' %}

{% block title %}Plat{% endblock %}

{% block body %}
<div class="fils" style="padding-left: 300px;">
    <h1>Plat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idplat</th>
                <td>{{ plat.idplat }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ plat.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ plat.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ plat.image }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ plat.prix }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ plat.categorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_plat_index') }}">back to list</a>

    <a href="{{ path('app_plat_edit', {'idplat': plat.idplat}) }}">edit</a>

    {{ include('plat/_delete_form.html.twig') }}
    </div>
{% endblock %}
